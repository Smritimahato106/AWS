version: 0.2

phases:
  install:
    commands:
      - echo "No dependencies to install"

  build:
    commands:
      - echo "Build started"
      - echo "Listing files"
      - ls -la

artifacts:
  files:
    - '**/*'
  base-directory: .
